<% provide(:title, title) %>
<h1>
	<div class="header-title">
		<span><%= title %></span>
	</div>	
	
	<% if admin_user? %>
		<%= link_to t(:new), path, class: "link_header btn btn-small", 
					id: "new_link_header" %>
	<% end %>
	
	<% if (defined? search) %>
		<%= render partial: 'shared/search_header', 
			locals:{ search: search} %>
	<% end %>
	
	<% if (defined? xlsx_download_path) %>
		<%= render partial: 'shared/xlsx_download', 
			locals:{ download_path: xlsx_download_path} %>
	<% end %>
	
	<% if (defined? xlsx_download_path_by_search) %>
		<%= render partial: 'shared/xlsx_download_by_search', 
			locals:{ download_path: xlsx_download_path_by_search} %>
	<% end %>
</h1>
