<%= form_for(@event_type) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  <div class="field">
    <%= f.label :name, t('activerecord.attributes.event_type.name') %><br>
    <%= f.text_field :name %>
  </div>
<% end %>
