<%= form_for(@event_type) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  <div class="field">
    <%= f.label :name, t('.name') %><br>
    <%= f.text_field :name %>
  </div>
<% end %>
