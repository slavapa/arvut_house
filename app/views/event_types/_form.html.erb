<%= form_for(@event_type) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  
  <div class="field">
    <%= f.label :name, t('activerecord.attributes.event_type.name') %>
    <%= f.text_field :name, class:'form-control' %>
  </div>
<% end %>
