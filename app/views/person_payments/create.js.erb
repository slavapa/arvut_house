$cur_tr = $('#tr_for_person_id_<%= @person.id %>');
$cur_tr.find('#td_present_person_<%= @person.id %>').html('<img alt="Vicon" src="<%= image_path('vicon.gif') %>">')
$cur_tr.find('.form_holder').
	html("<%= escape_javascript(render(partial: 'payments/remove_person_form', 
			locals: {payment: @payment, person: @person})) %>");
