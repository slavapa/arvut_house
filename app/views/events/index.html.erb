<%= render partial: 'shared/header_index', 
	locals:{ title: t('.list'),  path:new_event_path, search: @search} %>

<%= render partial: 'event_search', 
	locals:{ search: @search} %>
	
<%= will_paginate %>
<table class='table-bordered tabletable-hover table-index'>
  <thead>
    <tr>
      <th class='td_left short-size-td'><%=t('events.form.event_date')%></th>
      <th class='td_holder mm-size-td'><%=t(:name)%></th>
      <th class='td_holder  long-size-td'><%=t(:description)%></th>
    </tr>
  </thead>

  <tbody  class="tbody_events">
  	<%= render @events %>
  </tbody>
</table>