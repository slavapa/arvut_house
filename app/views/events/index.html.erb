<%= render partial: 'shared/header_index', 
	locals:{ title: t('.list'),  path:new_event_path} %>

<%= will_paginate %>
<table class='table-bordered tabletable-hover table-index'>
  <thead>
    <tr>
      <th><%=t('events.form.event_date')%></th>
      <th><%=t(:name)%></th>
      <th><%=t(:description)%></th>
    </tr>
  </thead>

  <tbody  class="tbody_events">
  	<%= render @events %>
  </tbody>
</table>