<%= form_for(@org_relation_status) do |f| %>
  <% if @org_relation_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@org_relation_status.errors.count, "error") %> prohibited this org_relation_status from being saved:</h2>

      <ul>
      <% @org_relation_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
