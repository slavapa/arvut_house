<%= form_for(@application_setup_type) do |f| %>
  <% if @application_setup_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_setup_type.errors.count, "error") %> prohibited this application_setup_type from being saved:</h2>

      <ul>
      <% @application_setup_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code_id %><br>
    <%= f.number_field :code_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
