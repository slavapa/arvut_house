$cur_tr = $('#tr_item_id_<%= @role.id %>');
$cur_tr.find('#td_item_exist_<%= @role.id %>').html('<img alt="Vicon" src="<%= image_path('vicon.gif') %>">')
$cur_tr.find('.form_holder').
	html("<%= escape_javascript(render(partial: 'people/remove_role_form', 
			locals: {person: @person, role: @role})) %>");
