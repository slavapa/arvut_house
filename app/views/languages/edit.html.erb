<%= render partial: 'shared/header_edit', locals:{ title: t('.editing'), 
	new_path: new_language_path, cur_item: @language,
	index_path_name: t('languages.index.list'), index_path: languages_path} %>

<%= render 'form' %>